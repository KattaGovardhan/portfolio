/** @type {import('next').NextConfig} */
const nextConfig = {
  allowedDevOrigins: [
    "http://192.168.31.238:3000", // your mobile device
  ],
  reactStrictMode: true,
};

export default nextConfig;
